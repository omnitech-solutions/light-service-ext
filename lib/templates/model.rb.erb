# frozen_string_literal: true

class <%= class_name %>
<% attributes.each do |attr| %>
  attr_accessor :<%= attr %>
<% end %>

  def initialize(<%= attributes.join(', ') %>)
<% attributes.each do |attr| %>
    @<%= attr %> = <%= attr %>
<% end %>
  end
end
